@* @model prjChatBot.Models.Menu *@
@model IEnumerable<prjChatBot.Models.Menu>
@{
    ViewData["Title"] = "Menu";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}
<style>
    a{
      text-decoration:none;
    }



    .card-img-top {
        width: 100%;
        height: auto; /* 高度自適應 */
        object-fit: contain; /* 保持圖片比例並完整顯示圖片 */
    }

    p{
        text-align:center;
    }
</style>
<h1>Menu</h1>
<a href="MenuCreate">新增</a>

@* @if (Model != null)
{
    <img src="@Url.Action("ShowImage", "Admin", new { id = Model.Id })" alt="Uploaded Image" />
    <p>@Model.TextContent</p>
}
else
{
    <p>目前沒有上傳的內容。</p>
} *@

<div class="container">
    <div class="row">
      @if (Model == null || !Model.Any())
    {
    <p>目前沒有上傳的內容。</p>
    }
    else
    {
        @foreach (var item in Model)
        {
            <div class="col-md-4 col-sm-6 mb-4">
                <div class="card">
                    <img src="@Url.Action("ShowImage", "Admin", new { id = item.Id })" class="card-img-top" alt="Image">
                    <div class="card-body">
                        <p class="card-text">@item.TextContent</p>
                            <form asp-action="Delete" asp-controller="Admin" method="post">
                            <input type="hidden" name="id" value="@item.Id" />
                            <button type="submit" class="btn btn-danger btn-sm">刪除</button>
                        </form>
                    </div>
                </div>
            </div>
        }
    }
    </div>
</div>


@* @if (Model == null || !Model.Any())
{
    <p>目前沒有上傳的內容。</p>
}
else
{
    @foreach (var item in Model)
    {
        <div>
            <img src="@Url.Action("ShowImage", "Admin", new { id = item.Id })" alt="Image" />
            <p>@item.TextContent</p>
            <form asp-action="Delete" asp-controller="Admin" method="post">
                <input type="hidden" name="id" value="@item.Id" />
                <button type="submit">刪除</button>
            </form>
        </div>
    }
} *@